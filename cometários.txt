#include <time.h>

    clock_t ultimoFrame = clock();

    clock_t agora = clock();

    double delta = (double)(agora - ultimoFrame) / CLOCKS_PER_SEC;
    ultimoFrame = agora;

esse método serve pra contar o tempo de espera de uma ação pra outra